#shell Permission command
There are different shell permission which give access to read, write and execute a file. they as follows:

chmod: modify file access rights

chgrp: change a file's group ownership

su: change to superuser temporarily

sudo: change to superuser temporarily

chown: change file ownership.